#!/bin/env bash

STATE=$1

if [ "$STATE" = "lock" ]; then
  pkill -f "idle-pre-lock"
  ~/.local/bin/idle-post-lock &
elif [ "$STATE" = "unlock" ]; then
  pkill -f "idle-post-lock"
  ~/.local/bin/idle-pre-lock &
fi
