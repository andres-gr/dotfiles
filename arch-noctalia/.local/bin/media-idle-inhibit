#!/usr/bin/env bash

playerctl --follow status 2>/dev/null | while read -r status; do
  case "$status" in
    "Playing")  noctalia msg caffeine-enable ;;
    *)          noctalia msg caffeine-disable ;;
  esac
done