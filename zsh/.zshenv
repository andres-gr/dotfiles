# ~/.zshenv
# Hyde-style minimal environment bootstrap

# ----------------------------------------
# Zsh config directory (XDG style)
# ----------------------------------------
export ZDOTDIR="$HOME/.config/zsh"

# ----------------------------------------
# Keep history in HOME (not XDG)
# ----------------------------------------
export HISTFILE="$HOME/.zsh_history"
