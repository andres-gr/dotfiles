# ── Input ─────────────────────────────────────────────────────────────────────
input {
    follow_mouse = 1
    kb_layout = us
    kb_variant = altgr-intl
    repeat_delay = 250
    repeat_rate = 40
    sensitivity = -0.4

    touchpad {
        natural_scroll = true
    }
}
